const teamHTML = (teamArray) => {

	//filter manager and pass to genrate manager
	
	
	// generate Manager

	// generate Eng

	//generate Intern

	// concatenate the teams and return the full html conent as string

}

module.exports = teamHTML